# Generated artifacts

This directory contains local convenience builds. The canonical public site is
generated into `docs/`, while the canonical implementation lives in `scripts/`
and `dashboard/`.

Files under `artifacts/site/` can be regenerated and are ignored unless already
tracked for historical reasons.
