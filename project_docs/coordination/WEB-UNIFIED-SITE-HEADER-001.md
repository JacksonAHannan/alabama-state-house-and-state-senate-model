# Task contract: WEB-UNIFIED-SITE-HEADER-001 — shared public masthead

- Accountable role: `web_product`
- Owner: `/root`
- Status: `complete`
- Objective: Replace page-specific public headers with one shared, deterministic masthead component across the forecast, CMO, ideology/caucuses, methodology, and compatibility pages.
- Acceptance checks: Every substantive public page renders the same portrait lockup, subtitle, navigation order, external links, dimensions, and breakpoint behavior; the correct page alone receives `aria-current`; no Candidate Atlas navigation item returns; embedded scripts remain byte-identical through theming; no overflow or console errors; focused tests and independent validation pass.
- Read scope: Current public builders, `scripts/site_brand.py`, shared theme, generated pages, and web tests.
- Write scope: `scripts/site_brand.py`; `dashboard/blue_oxblood_theme.css`; `scripts/tests/test_site_brand.py`; `docs/`; `project_docs/coordination/WEB-UNIFIED-SITE-HEADER-001.md`.
- Upstream inputs: Public site at commit `a89cee3`.
- Expected outputs: One canonical masthead generated by the shared theming layer and refreshed public HTML.
- Warehouse access: read-only.
- Handoff recipient: `validation_release`.

## Handoff

- The shared theming layer now replaces page-specific headers with one canonical public masthead.
- Six substantive pages share the same portrait, identity, ten-link navigation, spacing, and responsive behavior; only the correct active route differs.
- Candidate Atlas is absent from navigation, including on the legacy atlas route.
- Independent validation `VALIDATE-UNIFIED-SITE-HEADER-001` returned PASS; its focused suite passed 43/43.
