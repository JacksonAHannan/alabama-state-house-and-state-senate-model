# Task contract: WEB-IDEOLOGY-WAR-PUBLISH-012

- Accountable role: `web_product`
- Owner: `/root`
- Status: `complete`
- Objective: Publish the validated WAR-first ideology and caucus page and consistent site-method terminology to the GitHub Pages site.
- Acceptance checks: Independent validation passes; the scoped ideology and methods publication succeeds; published ideology output matches the validated local builder after the shared theme is applied; WAR is the first quantitative result; Split Ticket attribution is present; no public CQI label remains on the ideology page; ideology publication-contract and brand tests pass; release files are committed and pushed to the live branch.
- Read scope: Completed `WEB-IDEOLOGY-WAR-HEADLINE-011` release candidate, current site builders, current Git state, and independent validation report.
- Write scope: `docs/`; `artifacts/blue_oxblood_site/`; `project_docs/coordination/WEB-IDEOLOGY-WAR-PUBLISH-012.md`; `project_docs/coordination/active_tasks.csv`.
- Upstream inputs: `artifacts/site/ideology-performance.html`, current page builders, and validation task `VALIDATE-WEB-IDEOLOGY-WAR-012`.
- Expected outputs: Updated public site in `docs/`, staged mirror in `artifacts/blue_oxblood_site/`, a scoped release commit, and a successful push to the configured GitHub Pages branch.
- Warehouse mode: `read-only`
- Handoff recipient: User after successful remote push.

## Handoff

- Published `docs/ideology-performance.html` and `docs/methods.html` from the validated WAR-first builders; the publication copies match their staged mirrors byte-for-byte.
- Independent release approval is recorded in `project_docs/audits/IDEOLOGY_WAR_HEADLINE_VALIDATION.md`.
- Validation passed: 25 focused publication, page, brand, caucus, and historical-story tests; desktop and mobile browser checks found no horizontal overflow or console errors; all focusable chart marks have accessible names.
- Public terminology presents WAR as the headline candidate-quality result, preserves raw ticket overperformance as a separate measure, credits Split Ticket for the naming framework, and removes public CQI wording from this page.
- The unrelated pre-existing forecast-export byte mismatch was not included in this scoped publication and remains a separate downstream maintenance item.
